#include <engine.h>
#include <helpers/loop.h>
#include <menu.h>
#include <stdio.h>

/* full_run function
** is called if the user runs the program without parameters
*/

void			full_run(void)
{
	t_engine	eng;

	init_engine(&eng);
	engine_loop(&eng);
}
